import { ExerciceStoreDirective } from './exercice-store.directive';

describe('ExerciceStoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ExerciceStoreDirective();
    expect(directive).toBeTruthy();
  });
});
