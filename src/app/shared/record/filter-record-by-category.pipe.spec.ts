import { FilterRecordByCategoryPipe } from './filter-record-by-category.pipe';

describe('FilterRecordByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecordByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
