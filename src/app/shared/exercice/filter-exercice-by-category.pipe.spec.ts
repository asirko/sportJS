import { FilterExerciceByCategoryPipe } from './filter-exercice-by-category.pipe';

describe('FilterExerciceByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterExerciceByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
