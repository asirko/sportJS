import { ParseLineBreakPipe } from './parse-line-break.pipe';

describe('ParseLineBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseLineBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
